If you have already pushed your changes in the core repository to its remote development branch, you can tell the parent repo to fetch the latest remote tracking branch directly:

git submodule update --remote --merge